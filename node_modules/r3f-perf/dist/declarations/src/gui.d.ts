import { FC } from 'react';
import { PerfProps } from '.';
interface colors {
    [index: string]: string;
}
export declare const colorsGraph: (colorBlind: boolean | undefined) => colors;
/**
 * Performance profiler component
 */
declare const Gui: FC<PerfProps>;
export default Gui;
